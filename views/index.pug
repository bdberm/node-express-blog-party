extends layout.pug

block content
  h1 Index View!
  h3= subHeadVariable
  p.blurb Paragraph blurb
  section.separator This is a section
  ul
    each dog in dogArr
      li= "Name: " +  dog.name +", Breed: " + dog.breed 
